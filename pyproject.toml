[project]
name = "mcpproject"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mcp-use>=1.3.1",
    "nest-asyncio>=1.6.0",
]
